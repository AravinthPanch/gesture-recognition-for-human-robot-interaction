\begin{figure}
	[h] \centering 
	\includegraphics[width=120mm]{/content/nao-proxy.jpg} \caption{NAOqi modules form a tree of methods attached to modules, and modules attached to a broker. Thus, NAOqi Broker (proxy) can be used to remotely invoke any attached methods. \cite{8} } \label{fg:nao:proxy} 
\end{figure}
